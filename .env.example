# =============================================================================
# better-nexbase — Environment Variables
# =============================================================================
# Copy this file to .env (or the relevant .env.local files) and fill in values.
# See apps/web/.env.example and packages/backend/.env.example for per-package files.

# ── Web App (apps/web/) ──────────────────────────────────────────────────────

# Convex
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# WorkOS AuthKit
VITE_WORKOS_CLIENT_ID=client_xxxxxxxxx
VITE_WORKOS_REDIRECT_URI=http://localhost:3001/callback

# ── Convex Backend (packages/backend/) ───────────────────────────────────────

# WorkOS
WORKOS_CLIENT_ID=client_xxxxxxxxx

# App URL (used for invite links, billing redirects, etc.)
SITE_URL=http://localhost:3001

# Organization feature flags (can be overridden in appConfigs at runtime)
ORG_ENABLED=false
ORG_FEATURE_RBAC_STRICT=true
ORG_FEATURE_BILLING_ENFORCEMENT=false
ORG_FEATURE_INVITE_EMAILS=false
ORG_FEATURE_HARD_LOCKING=false
ORG_FEATURE_WORKOS_MIRROR_WRITES=false

# Autumn (billing)
AUTUMN_SECRET_KEY=am_sk_xxx

# Resend (email)
RESEND_API_KEY=
RESEND_FROM_EMAIL=noreply@yourdomain.com
RESEND_TEST_MODE=true
RESEND_WEBHOOK_SECRET=
